# Database
DB_PASSWORD=change-me-in-production

# Server
PORT=3000
NODE_ENV=production

# File Storage
STORAGE_ROOT=/data/uploads
MAX_FILE_SIZE=104857600

# File Lifecycle
FILE_EXPIRY_DAYS=7

# OIDC / Authentik
OIDC_ISSUER=https://auth.snir.sh/application/o/file-share/
OIDC_JWKS_URI=https://auth.snir.sh/application/o/file-share/jwks/
OIDC_CLIENT_ID=file-share

# Frontend OIDC (used at build time via Vite)
VITE_OIDC_ISSUER=https://auth.snir.sh/application/o/file-share/
VITE_OIDC_CLIENT_ID=file-share
